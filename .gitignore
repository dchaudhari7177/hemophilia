# virtualenv
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ipynb_checkpoints/

# large binaries kept out of history
*.xlsx

# transient run log; the JSON artefacts in reports/ are the record
reports/_*.log
reports/run.log

# The hybrid artefact is ~75MB because isotonic calibration keeps one copy of a
# 600-tree forest per fold. It is regenerated in a couple of minutes by
# .devtools/eval_hybrid.py, so it does not belong in history.
models/hybrid_model.joblib

# OS
Thumbs.db
.DS_Store
